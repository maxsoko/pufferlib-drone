# VQ2-SF068 result — rejected

SF068 completed all `48` preregistered exact two-gate PPO updates and
`392,713` valid native steps. No deterministic evaluation completed Gate 2.
The stored update-8 checkpoint passed Gate 1 on all `128` exact agents with
zero crash, but timed out with a Gate-2 closest range of `8.540204 m`. The
final update likewise passed only Gate 1 and missed Gate 2 at `7.774087 m`.

Exploration did produce a useful but non-promotable diagnostic: one sampled
update-12 trajectory reached the Gate-2 plane at `0.053754 m` radial error.
The deterministic mean never consolidated that behavior. Whole-actor PPO also
progressively perturbed the already-solved Gate-1 pathway, so SF068 rejects
global fine-tuning and supports a phase-local update boundary.

The run then hit a reporting-only `TypeError` after its final evaluation
because a local recurrent-output value shadowed the output-directory argument.
The run was not repeated. The persisted checkpoint SHA-256 is
`90fa709467bdc0221cb9e96920d86609f94d9c4dab0a6df57e330422ceba6e95`;
the concise recovery report is `report_recovered.json`. The reporting variable
was renamed, and checkpoint ranking now treats an unsampled native terminal
crossing as missing rather than as zero radial error.

SF068 sent zero FlightSim packets, executed zero teacher actions, exported no
critic, did not access N712, and authorizes neither shadow nor live control.
