# LC091 phase-7 successful-trajectory recapture preregistration

LC090's fresh 512-run corpus is rejected: only four agents entered phase 7 and
all four failed. LC087's already-promoted paired run at seed 431870 proves that
its selected checkpoint had exactly three phase-7 entrants: one stopped at raw
index 7, one at 8, and one at 9. The underlying phase-7 records were not saved.

Replay that source-locked 128-seed selected-checkpoint cohort once for training
data capture only. Record legal recurrent features, Puffer pre-tanh actions,
and training-only teacher queries exactly while held public index is 7. The
LC087 checkpoint owns every plant action; use 32 native threads and the full
12,000-step horizon. The run is admitted only if all 128 episodes complete and
the known summary reproduces exactly as three queried agents, two successes,
and one failure, with at least 1,000 records and all transport/safety checks.

This is recapture of previously admitted seeds, not fresh validation. Its data
may be combined with LC090's four fresh failures for outcome analysis and one
offline intervention, but it may not support a promotion by itself. No
FlightSim or Submission authority is granted.
