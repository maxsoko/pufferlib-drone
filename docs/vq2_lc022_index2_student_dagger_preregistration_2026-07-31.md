# VQ2 LC022 public-index-2 student-state labels — 2026-07-31

LC021 selects the cumulative through-phase-5 rollback. In its paired 32-run
screen, 18 episodes stop at official-progress proxy index 2 while only 14
advance, making phase 2 the first dominant weak phase. Collect a compact
training-only oracle label corpus on states reached by that exact Puffer.

Run 256 full-start 24-gate native episodes at fresh seed `431220`, 32 threads,
and at most 5,000 steps. The deterministic recurrent Puffer owns every plant
action. Query the training-only native oracle only while held public progress
equals 2; record recurrent hidden features, base logits, labels, and provenance.
The teacher must execute exactly zero plant actions. Require at least 100,000
finite in-envelope phase-2 records, exact public-status transport, exact action
history, all episodes, and no hard native fault.

LC022 sends zero FlightSim packets. The dataset authorizes only a separately
source-locked phase-2 fitting/bracketing experiment; it grants no replay,
shadow, live, or Submission authority.
