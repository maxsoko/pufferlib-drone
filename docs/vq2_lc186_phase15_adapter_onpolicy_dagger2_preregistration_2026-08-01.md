# VQ2 LC186 second adapter-owned phase-15 DAgger preregistration

LC185 rejected LC184 at 0/128 raw-index-16 passes. Repeat LC183's paired
collection contract with two independent LC184 Puffers over 256 rows each.
Use LC184 unchanged in control and the alignment oracle only as a training
plant rescue at public phase 15 in intervention. Save the frozen 256-state base
and base-only logits with oracle targets on both branches; never save adapter
state as a student input.

Admit only 0/256 control, 256/256 rescue, all 512 rows represented at phase 15,
finite in-envelope labels, and exact transport/pairing. No FlightSim packet is
sent and no live or Submission authority is granted.
