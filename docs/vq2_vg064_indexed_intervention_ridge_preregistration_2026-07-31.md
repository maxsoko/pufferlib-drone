# VQ2 VG064 indexed intervention-feature distillation — 2026-07-31

Run one source-locked offline fit tagged
`vq2_vg064_indexed_intervention_ridge_001`. VG063 admits 4,666,436 compact
warmed features from `512/512` collision-free completed variable courses with
public-index coverage 1--11. Its feature/report/admission SHA-256 values are
`ccfc07e5...`/`d74f16bd...`/`47c1e107...`.

Keep every VG033 encoder, recurrent, decoder, standard-deviation, and phase
embedding parameter exact. Fit only the learned 4x256 residual head associated
with each public index 1 through 11; heads 0 and 12--16 remain exact zero. The
result remains one recurrent full-output Puffer actor. Public progress selects
an internal learned parameter row; no native/classical action, blend, clip,
fallback, or external controller is present.

Reserve agents whose index modulo 8 is zero for validation. On the other
agents, accumulate float64 normal equations from float32-expanded warmed
features and pre-tanh oracle residual targets. For each phase independently,
screen ridge values `1e-4,1e-3,1e-2,1e-1,1,10,100` by held-agent post-tanh
four-action MSE. Clip only the offline inverse-tanh label to `+-0.999`; no
runtime output clipping is introduced.

Numerically admit only if every phase 1--11 has train/validation mass, every
selected phase improves action MSE by at least 1.25x, every head L2 is finite
and at most 64, all VG033 parameters are exact, and non-target heads are zero.
Numerical admission authorizes only a fresh teacher-free scale/rollout ladder.

Before launch bind the pushed commit, runtime, parent checkpoint/report,
VG063 report/features/admission, 48-hour goal, model, collector dtype, trainer,
runner, test, and zero-authority fields. Require the retained RTX 4090 worker
and focused tests. Frozen new source hashes:

- trainer: `448a3696732d77e10d88135face8c9c82c493324946459e04a017709945b3ad5`
- runner: `1797924c6f5f54a4ec8d5d00e80e92c95614c19b414363997ffd4a2836d1c81c`
- test: `09a94bc75919c6f02884935b4aaf8e0f5c1e5fce9144e7b10bc7e88d41bd84ed`

Teacher plant/runtime actions, FlightSim packets, shadow, Training, Submission,
and sealed-test access are zero. VQ2 Submission remains forbidden.
