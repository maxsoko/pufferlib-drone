# VQ2-SF051 result — 2026-07-28

SF051 is rejected and must not be retried unchanged. Frozen SF050 reached the
exact measured two-gate screen's numerical admission but failed closed-loop:
`0/512` instances passed Gate 1, and all `512` terminated in a low crash after
about `486` steps (`7.588 s` native mean crash time). Native mean maximum
progress was `0.5733` toward Gate 1.

The failure is not an execution or ABI discrepancy. The actor consumed exactly
4,119 legal/public values, emitted all four actions, used teacher blend zero,
had zero nonfinite or envelope violations, and delivered actions with maximum
error `0.0`. Held phase never changed, so the failure precedes and is
independent of the new public phase transition.

Report SHA-256:
`56a260f04d9204f12e82ec5d4c3cee0035cea88a0e76224ea5bb7617b2efe6c9`.
The result diagnoses closed-loop covariate drift that validation action MSE did
not capture. The next permitted action is a fresh exact-course DAgger
collection driven by SF050, aggregated with the clean SF049 oracle prefix.
No FlightSim packet was sent and no live authorization changed.
