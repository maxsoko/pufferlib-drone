# VQ2 VG017 four-source recurrent refit preregistration — 2026-07-30

## One-fit decision and immutable inputs

Run one epoch-resumable fit tagged
`vq2_vg017_variable_gate_four_source_refit_001`, seed `429080`, starting from
admitted VG014 checkpoint SHA-256
`60d69a1f80a1551b190cd4b517dfa029bc3dbe48728a853a47b658df0866d79e`.
Its report SHA-256 is
`067d9c198c14959dbb7221d34f9fec0f065a3cee41043e3ce0ff1f9a7bbf0a16`
and admission evidence SHA-256 is
`073fc31e143e2d8e61e40cead685698c47e012b7153b32622bd4dc202fbcfa72`.

Use only four admitted legal-observation datasets:

- VG003 clean anchors, report/metadata SHA
  `b8069d05...`/`7a5d6359...`;
- VG009 first failure distribution, report/metadata SHA
  `72c1e41d...`/`da20bd4e...`;
- VG013-recovered VG012 failure distribution, recovery-report/metadata SHA
  `38358a58...`/`e7bfdba1...`; and
- VG016 early-transition distribution, report/metadata SHA
  `bcd86c8b...`/`4f075d2d...`, bound by admission SHA `eb6c4f56...`.

VG007/VG008 remain quarantined. No failed collector state is resumed or
rewritten. No new labels or native rollout are generated by this fit.

## Fixed optimization contract

Every optimizer update pairs one recurrent chunk from every source. Normalize
each source loss independently, then mix exact per-update objective weights:

- VG003 clean: `0.40`;
- VG009: `0.15`;
- recovered VG012: `0.15`; and
- VG016: `0.30`.

The new distribution receives twice either older failure-source weight while
clean remains the largest single source. This focuses the observed Gate-2
transition without allowing VG016's raw record count to determine the
objective or dropping either earlier failure anchor.

Use twelve epochs, four agents per source per paired chunk, 256-step recurrent
BPTT, and three optimizer exposures for every paired chunk containing any
public-phase transition. The clean stream defines each epoch; every DAgger
stream cycles only at episode-batch boundaries. Use AdamW learning rate
`2e-5`, weight decay `1e-5`, gradient cap `1.0`, temporal smoothness `1e-4`,
and action weights `(1,1,4,1)`. Preserve genuine episode boundaries and reset
the recurrent state only at those boundaries.

## Validation and numerical admission

Reserve the final 32 VG003 agents and final 64 agents from each DAgger source
for disjoint deterministic validation. Evaluate unchanged VG014 as epoch zero.
Select exactly the minimum weighted
`0.40/0.15/0.15/0.30` validation checkpoint across epoch zero and all twelve
children.

Numerical admission requires all of the following:

- a child epoch, not unchanged VG014, is selected;
- selected four-source weighted MSE is finite and lower than epoch zero;
- selected VG016 weighted MSE is lower than epoch zero;
- selected clean and VG009 weighted MSE are each at most `0.02`;
- selected recovered-VG012 weighted MSE is at most `0.10`;
- every epoch has exact source objective weights; and
- minimum transition-window exposure is at least `3.0x`.

These are fit checks only. They cannot admit a policy for live use. A numerical
admission authorizes one separately preregistered teacher-free deterministic
screen on fresh courses; numerical rejection authorizes diagnosis only.

## Source identity and resume

Before epoch zero, `training_state.pt` binds the Git commit, all four dataset
reports/metadata/admissions, parent checkpoint/report/admission, goal prompt,
trainer/helper/runner/preregistration sources, runtime, splits, fixed config,
phase audits, safety fields, model/optimizer/scaler state, and Python/NumPy/
Torch/CUDA RNG state. Resume is permitted only from that exact identity at an
epoch boundary. A completed output verifies its checkpoint/report/state chain
and cannot be overwritten.

The remote bootstrap requires the exact pushed commit, clean tracked tree,
existing source-locked virtual environment, supported CUDA GPU, at least 32
visible CPUs, approximately 64 GiB RAM, 20 GiB free disk, Clang/OpenMP, and
ccache. It runs both native regression suites, a fresh float32 CUDA build/ABI
check, and focused/adjacent tests before epoch zero.

## Frozen source surface

- `scripts/train_vq2_variable_gate_four_source_refit.py` —
  `aeb629cd30a524f941e677214c31983c25874ae4d5afa7b21d555304b4f3fc93`
- `scripts/run_vq2_vg017_vast.sh` —
  `3a06b1843597057d85cf198e11156020eb743ba794b044a9b68d7e8c23698af9`
- `tests/test_train_vq2_variable_gate_four_source_refit.py` —
  `670c6dd91dd61843cf72a7f9e781069adb4ff5f3c42967e1286c2bb3b960176f`
- `scripts/train_vq2_variable_gate_three_source_refit.py` —
  `4795040a359b9972acae93c98cd5126ec3504c137836aabdb1722bbc3cb3bf7a`
- `scripts/train_vq2_variable_gate_recurrent_bc.py` —
  `a3e4fb3e6b553d886044fed04a2e132b9738a3fd2eb6ca34ad3a27fab634184f`
- `scripts/train_vq2_variable_gate_dagger_refit.py` —
  `afc7ea57da09365c3f6f98ef3f2faaf520dc16c685548645be96f743a08d00fb`
- `scripts/train_vq2_recurrent_bc.py` —
  `a024a6fc387fbf734194d9505a9ed23e2b649b01f1ed9fe596ebc9a317b97583`
- `pufferlib/vq2_informed.py` —
  `35b63ed73891aa7e13ecc3888507894534c8fdf701ebfb7f6f8a52470ec28c2a`
- `pufferlib/vq2_recurrent.py` —
  `5ccbee9ce7c98a3a9e9eae627d9f5ea86b3d1c29707eef1921d8c5f56c196199`
- `pufferlib/vq2_recurrent_phase.py` —
  `a15e28b2147ea485ea2ce7d3a889027fe0878e4baaa146b8f71d5b86f18a05aa`

The source identity also binds this preregistration, all exact input artifacts
and admissions, and the persistent goal prompt before epoch zero.

## Safety and next authority

The actor input remains the legal 4,119-value public ABI. Teacher blend,
teacher plant actions, privileged actor inputs, FlightSim packets, sealed N712
accesses, Windows shadow, Training, and Submission authorization are zero.
Even numerical admission authorizes only a fresh offline teacher-free screen.
