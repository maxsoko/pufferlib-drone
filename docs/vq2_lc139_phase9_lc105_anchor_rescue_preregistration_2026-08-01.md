# VQ2 LC139 LC105 phase-9 anchor/rescue preregistration

LC138 rejects the LC123 phase-8 fit with no raw-9 change. Return to frozen
LC105, the safe whole-Puffer frontier, which has one deterministic raw-9
trajectory in the established 128-seed screen.

Run one 256-environment paired native vector from LC105 at seed `432050`, with
two identical 128-seed groups in one complete saved-form recurrent Puffer
actor. Stop at raw index 10 or native terminal. In group 0, execute LC105
throughout and record phase-9 hidden state plus LC105 action as diagnostic
anchor. In group 1, execute LC105 through the exact same prefix and replace
only held phase-9 actions with the training-only alignment oracle, recording
those oracle rescue labels. Outside phase 9, both groups execute complete LC105
actions.

Admit only if the groups begin byte-identically, control reproduces exactly one
raw-9 entrant and zero raw-10 passes, intervention adds exactly one paired
raw-10 pass with zero loss, both groups contribute one phase-9 trajectory,
features are phase-9-only and finite, and transport is exact. This one-pair
dataset is not a statistical promotion result; it only authorizes a bounded
in-distribution fit followed by a teacher-free closed-loop screen.

No student update, FlightSim packet, or Submission action occurs in LC139.
The randomized proxy contains 24 gates; the official course is approximately
20 gates or more and official finish status remains authoritative.
