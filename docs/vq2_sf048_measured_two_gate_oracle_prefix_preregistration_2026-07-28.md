# VQ2-SF048 extended measured two-gate oracle prefix — 2026-07-28

Tag: `vq2_sf048_measured_two_gate_oracle_prefix_64`

SF047's 768-step bound did not admit the measured two-gate oracle. Repeat on a
fresh tag/seed (`42048`) with only the bound changed to 1,600 steps (25 s).
Keep all geometry, exact apertures, zero randomization, held public phase,
oracle parity, safety predicates, and legal storage rules unchanged. Require
all 64 agents to reach held phase `>=2/6`.

This sends zero FlightSim packets, does not access N712, and cannot authorize
a policy screen, shadow, bounded attempt, or Submission.

