# VQ2 LC232 fitted phase-2 exact NumPy batch-1 preregistration

LC232 is a teacher-free native closed-loop screen of the immutable LC231 NumPy
export. Eight independent recurrent policy objects each receive one legal
4118-value observation plus held public progress and emit the entire four-action
vector. There is no batched policy arithmetic, teacher, privileged actor input,
action blend, analytic override, or FlightSim packet.

Stage 1 repeats native seed index 287 across eight independent policy objects,
matching LC229's deterministic 0/8 Gate-3 failure context. It uses a 10,000-step
bound and requires 8/8 Gate-3 passes, zero pre-target terminal, exact command
transport, finite in-envelope actions, and ordered 4 Hz progress.

Only if Stage 1 passes may the runner start Stage 2, which preserves the exact
same deployment arithmetic and requires 8/8 uninterrupted raw-index-24 proxy
completions under 45,000 steps. Passing both stages authorizes only N399-prefix
replay and a later zero-command shadow decision. FlightSim control remains
frozen. VQ2 Submission remains forbidden.
