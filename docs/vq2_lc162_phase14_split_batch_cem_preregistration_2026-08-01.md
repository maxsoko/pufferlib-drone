# VQ2 LC162 phase-14 split-batch CEM preregistration

LC161 establishes LC160 as a teacher-free raw-14 source-trajectory parent.
Search one constant phase-14 pre-tanh residual over 512 exact seed-15 copies,
using two independent 256-row Puffer actors, no more than two generations, and
a raw-index-15 or 24,000-step stop.

Admit only if at least one trajectory reaches raw index 15, all 512 query phase
14, transport is exact, and only the phase-14 output-bias row changes. Otherwise
move to the proven two-iteration DAgger rescue. This is source-only evidence.

No FlightSim packets are sent and no live or Submission authority is granted.
The proxy has 24 gates; the official course has approximately 20 gates or more,
and only official `race_finish_time_ns >= 0` proves an official lap.
