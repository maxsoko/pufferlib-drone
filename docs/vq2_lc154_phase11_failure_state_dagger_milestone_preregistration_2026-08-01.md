# VQ2 LC154 phase-11 failure-state DAgger milestone preregistration

Screen source-locked LC153 against its LC148 parent on the exact seed-15
source trajectory. Use one 256-row native vector with 128 identical rows per
candidate and two complete recurrent Puffer actors, each executing a
source-matched 256-row pair. LC153 may differ only in the four indexed
phase-11 residual parameter rows.

Select LC153 only if all 128 candidate rows reach raw index 12, LC148 reaches
none, paired losses are zero, and every transport and progress invariant
passes. Passing is source-trajectory evidence only; broad independent
admission remains mandatory before any live consideration.

This is teacher-free offline evidence only. It sends no FlightSim packet and
grants no live or Submission authority. The proxy has 24 gates; direct
simulator inspection indicates approximately 20 official gates or more, and
only official `race_finish_time_ns >= 0` proves an official lap finish.
