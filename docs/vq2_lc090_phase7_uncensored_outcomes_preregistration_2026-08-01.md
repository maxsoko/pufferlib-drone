# LC090 phase-7 uncensored outcome collection preregistration

LC088's parity screen improved raw-index-8 reach, but the fresh LC089
confirmation produced only one additional pass rather than the required two.
Reject that checkpoint change without retry. LC089 nevertheless shows why the
next intervention must be measured: only four of 128 LC087 trajectories entered
phase 7, and only one passed to raw index 8.

Run one fresh deterministic batch of 512 LC087-owned trajectories on the fixed
24-gate proxy, seed 431900, 32 native threads, and the ordinary 12,000-step
horizon. Query the training-only alignment teacher only while held public index
is exactly 7, but never execute its actions and do not stop on record count.
The recurrent LC087 Puffer owns every plant action and all 512 trajectories
must reach terminal state.

Admission requires at least 10,000 phase-7 records from at least eight agents,
zero censored queried trajectories, at least two phase-7 successes and two
failures, exact action/history/progress transport, finite in-envelope labels,
and zero hard native faults. This corpus authorizes measured outcome analysis
and one source-locked phase-7 offline intervention. It grants no FlightSim or
Submission authority, and no native state or outcome label may become a runtime
input.
