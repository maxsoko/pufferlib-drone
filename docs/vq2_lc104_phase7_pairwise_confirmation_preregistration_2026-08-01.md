# LC104 phase-7 pairwise confirmation preregistration

LC103's corrected 256-row actor context selected the exact LC101 endpoint. It
increased raw-index-8 passes from 1/128 to 2/128 with one paired gain, zero
losses, and one fewer pre-target terminal. Scale 2 tied, so the smaller exact
endpoint won the preregistered ordering.

Run one independent 128-pair comparison at seed 432040 with the same 256-row
actor execution contract and 12,000-step limit. Load LC094 and exact LC101 as
complete saved-form recurrent Puffer policies; each actor executes all 256
rows and its group selects a complete action vector. No post-forward surgery,
teacher action, outcome label, native-state action, or analytic override is
allowed.

Confirm only if LC101 adds at least one raw-index-8 pass, adds no pre-target
terminal, and passes exact action/progress transport. Confirmation authorizes
one serialization-exact full 24-gate proxy comparison only. It grants no
FlightSim or Submission authority.
