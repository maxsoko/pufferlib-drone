# VQ2 LC129 expanded phase-8/9 rescue corpus preregistration

LC125 proves one LC123-owned phase-8/9 oracle rescue but its three queried
trajectories contain only one success. LC127/LC128's subsequent teacher-free
on-policy phase-6 PPO branch is rejected because its deterministic mean removes
all raw-8/raw-9 reach. Return to the measured recoverable distribution, without
fitting the rejected LC125 corpus alone.

Run one expanded paired diagnostic with 256 distinct native seeds per group at
the same course configuration. Use 512 environments in one native vector, but
execute the two paired groups through two independent complete 256-row LC123
Puffer actors so actor-batch numerics remain identical to LC124/LC125. The
control group is Puffer-only. The intervention group executes the training-only
alignment oracle only at held phases 8--9, targeting raw index 10 over 12,000
steps and 24 proxy gates.

Capture all intervention features, but aggregate only the previously unseen
relative seeds 128--255 with LC125's original relative seeds 0--127. Preserve
unique agent identities. Admit the combined corpus only if transport is exact,
the expanded intervention has at least one paired raw-10 gain with zero loss,
all stored records are finite, in-envelope, and phase-8/9-only, and the combined
whole-trajectory outcomes contain at least two successes and two failures.
This single expanded run replaces a sequential multi-seed ladder and avoids
duplicate training rows.

The resulting data may authorize one source-locked whole-Puffer fit only. It
does not promote LC123. The official VQ2 course remains approximately 20 gates
or more by direct simulator inspection, with official finish status
authoritative. Send zero FlightSim packets and keep Submission forbidden.
