# VQ2 LC212 stacked-adapter output PPO preregistration

LC202--LC210 show that three supervised continuation-adapter fits regress the
deterministic LC189 raw-17 frontier to raw 16. LC211 then exhausts 1,536
constant phase-16 residual trajectories with no raw-18 completion. The next
smallest state-dependent experiment is therefore a closed-loop update of the
continuation adapter's four-by-64 output weight and four-value bias.

Run six teacher-free PPO rollouts of 512 exact seed-15 trajectories. Execute
them as two independent complete 256-row recurrent LC209 Puffers. Exploration
and recorded policy queries apply only at public phase 16. Native geometry may
provide training-only camera-alignment and ordered-progress rewards, but no
teacher action may enter the plant, labels, checkpoint, or runtime. Freeze the
base Puffer, phase-15 adapter, continuation GRU, and every non-output tensor
bit-exactly. Rank checkpoints by raw-18 passes and then ordered progress.

LC209 is only a training seed. LC189 remains the trusted 17/24 controller.
Even a positive LC212 result authorizes only one deterministic exact-context
offline screen. It authorizes no FlightSim or Submission traffic.
