# VQ2 LC234 phase-2 sequence exact NumPy batch-1 preregistration

LC234 is a teacher-free native closed-loop admission screen of immutable LC233.
Eight independent recurrent policy objects each receive one legal observation
and emit the complete four-action vector. Every LC216 recurrent state advances
continuously; held public progress selects the checkpointed phase-2 sequence
head and later LC216 sequence head. There is no teacher, privileged actor input,
action blend, analytic override, or FlightSim packet.

Stage 1 repeats native seed 287 across eight exact NumPy batch-1 policy objects
under 10,000 steps. Admission requires 8/8 Gate-3 passes, zero pre-target
terminal, exact transport, ordered 4 Hz progress, and finite in-envelope actions.
Only then may Stage 2 require 8/8 uninterrupted raw-index-24 proxy completions
under 45,000 steps using the same deployment arithmetic.

Passing both stages authorizes only N399-prefix replay and subsequent
zero-command shadow work. FlightSim control stays frozen until those separate
gates pass. VQ2 Submission remains forbidden.
