# VQ2 LC143 phase-10 split-batch CEM preregistration

LC142 causally confirms that LC141 converts LC105's sole raw-9 trajectory into
one raw-10 pass with zero paired loss.  Use the source-locked LC141 checkpoint
as an offline development parent while retaining LC105 as the safe frontier.

Replicate native seed index 15 across 512 environments and execute two complete
256-row recurrent LC141 Puffer actors.  Extend the offline limit from 12,000 to
15,000 steps because LC142 reaches raw index 10 at step 11,962.  Before phase
10 every action is exact LC141.  During held public phase 10, search one bounded
trajectory-constant four-channel pre-tanh output-bias residual using at most
four teacher-free CEM generations.  Stop at the first generation with a raw-11
pass.

Admit only if every row begins byte-identically, all 512 rows query phase 10,
transport and phase sequencing are exact, at least one candidate reaches raw
11, and checkpoint surgery changes only the indexed phase-10 residual
output-bias row.  Admission grants deterministic offline screen authority only.

The native proxy has 24 gates.  Direct simulator inspection indicates that the
official course has approximately 20 gates or more; official finish status is
the only official lap proof.  No FlightSim packet or Submission action occurs.
