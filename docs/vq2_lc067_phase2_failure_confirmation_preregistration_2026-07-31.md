# LC067 failure-conditioned phase-2 confirmation preregistration

LC066 selects failure-score-conditioned pitch coefficient `-0.01`, improving
Gate-3 passes from 8/32 to 9/32 with two paired gains and one loss. Smaller
coefficients regress, indicating a possible threshold rather than a smooth
imitation direction.

LC067 uses new seed `431670`, four paired groups of 64, the same 24-gate proxy,
and the 3,500-step milestone. Compare LC062 against conditioned pitch
coefficients `-0.0075`, `-0.01`, and `-0.015`. A candidate must gain at least
two Gate-3 passes, not increase pre-Gate-3 terminals, and pass exact transport.
Any winner still needs a fresh-seed full 24-gate screen. No FlightSim or
Submission authority exists.
