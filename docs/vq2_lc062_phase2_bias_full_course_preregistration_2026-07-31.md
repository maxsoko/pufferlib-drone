# LC062 corrected phase-2 bias full-course preregistration

LC061 completed its paired command-free 24-gate vector and reached its
preregistered checkpoint-promotion branch, but emitted no artifact because the
checkpoint-save helper arguments were reversed. LC061 is rejected without an
unchanged retry.

LC062 corrects only that call and uses a unique tag/schema. It repeats the
LC061 contract exactly: frozen LC048 versus its `-0.0025` phase-2 pitch-bias
whole-Puffer candidate, 64 paired episodes each, seed `431610`, 24 gates,
12,000 steps, 32 threads, held 4 Hz public progress, and no teacher,
FlightSim, or Submission authority. The original LC061 promotion predicates
remain unchanged.
