# LC113 phase-9-only full-residual screen preregistration

LC112 shows that every joint phase-8/9 endpoint scale from `.01` through `.10`
converts LC105's sole raw-index-9 trajectory into a raw-index-8 stop. The
phase-8 edit prevents the newly trained phase-9 row from being exercised.

Repeat the same scales `0,.01,.03,.10`, but preserve every LC105 phase-8 tensor
bit-exact and interpolate only LC111's four phase-9 residual tensors. Keep the
128-pair, pairwise-256 complete-Puffer execution, seed 432130, raw-index-10
target, and 12,000-step bound. This is a new causal isolation, not an unchanged
LC112 retry. No teacher, native-state action, post-forward surgery, or analytic
override executes.

Select only a nonzero scale that creates one raw-index-10 pass, adds no
pre-target terminal, and passes exact transport. Selection authorizes one
independent confirmation only; FlightSim and Submission remain forbidden.
