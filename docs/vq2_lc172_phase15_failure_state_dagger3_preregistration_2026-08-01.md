# VQ2 LC172 phase-15 third DAgger preregistration

LC170 and LC171 reject the second phase-15 fit and its interpolation direction,
but LC169 terminates earlier than LC166 and therefore exposes a shifted on-policy
failure distribution. Repeat the admitted pairing from that exact LC169 state:
256 unassisted controls and 256 training-only alignment-oracle interventions,
with offline labels on both halves.

Admit only if control is 0/256, intervention is 256/256, all 512 rows query
phase 15, and transport, pairing, and labels pass. This sends no FlightSim
packet and grants no live or Submission authority. The official VQ2 course is
approximately 20+ gates by direct inspection; this is only offline raw index 16
of the fixed 24-gate proxy.
