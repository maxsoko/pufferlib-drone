# LC088 phase-7 parity-bias scale bracket preregistration

LC087 is the promoted offline frontier. On a paired 128-seed 24-gate screen it
increased mean progress from `3.3828125` to `3.421875`, increased raw-index-7
reach from one to three, reached raw index 9, and did not increase crash rate.
It added the measured half-scale success-action bias
`[+0.005,-0.025,+0.0125,0]` only to phase 6.

The native 24-gate proxy alternates gate lateral position by index. Test the
causal parity-transfer hypothesis at phase 7 by mirroring roll while preserving
pitch and thrust. Screen scales `0,.25,.5,.75,1,1.5,2,3` of the full direction
`[+0.01,+0.05,+0.025,0]` on eight exact 32-seed groups, fresh seed 431880,
one CUDA context, one native vector, target raw index 8, and 12,000 steps.
Only the indexed phase-7 Puffer output-bias row may change.

Select only a nonzero scale with at least one additional paired raw-index-8
pass, no added pre-target terminal, exact transport, and zero action-envelope
violations. Ties prefer more passes, fewer terminals, then smaller parameter
displacement. Selection authorizes only a larger fresh-seed confirmation.
Teacher actions, privileged runtime state, FlightSim packets, and Submission
authority are all forbidden.
