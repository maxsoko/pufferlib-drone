# LC116 exact phase-8/9 training-oracle rescue preregistration

LC115 proves that LC105's exact raw-9 entrant is already unrecoverable by the
training-only alignment oracle: 720 exact oracle plant actions leave the paired
candidate at 0/128 for raw progress 10. The same governed teacher is separately
proven over the complete 24-gate proxy from healthy starts, so the next bounded
question is whether intervention must begin one transition earlier.

Repeat LC115's saved LC105 actor, 256-agent full batch, duplicated 128-seed
cohorts, seed `432050`, 24 proxy gates, CUDA inference, 32 CPU threads, and
12,000-step raw-10 stop. Keep the first cohort pure Puffer. In the second cohort
only, replace the complete four-action output with the training-only alignment
oracle while held public progress is 8 or 9. The Puffer recurrent state still
advances on every legal observation and previous executed action.

A rescue requires at least one paired raw-10 gain, no paired loss, no added
pre-target terminal, exact action/progress transport, and no teacher action
outside candidate phases 8--9. A rescue authorizes only exact intervention
collection and whole-Puffer distillation. It does not admit a checkpoint or
authorize FlightSim/Submission. If it fails, reject this teacher through phase
8 and move the causal-horizon diagnosis earlier without fitting its labels.
