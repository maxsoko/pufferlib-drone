# VQ2 LC215 raw-18 to raw-24 sequence preregistration

LC214 promotes LC213 as a 128/128 teacher-free raw-18 checkpoint. Run one
paired 512-agent native collection from that exact parent. The first 256 agents
remain unassisted controls. For the second 256 agents only, permit the
training-only alignment oracle to emit plant actions during proxy phases
18--23. Target raw index 24 and stop by 45,000 native steps.

Admit the dataset only if all 256 intervention agents reach raw 24, controls do
not, every phase 18--23 contributes source-locked records, exact pairing and
transport pass, and teacher actions remain finite and normalized. Runtime and
FlightSim authority remain zero. A passing result authorizes only offline
concatenation into a Puffer checkpoint and one all-24 screen.
