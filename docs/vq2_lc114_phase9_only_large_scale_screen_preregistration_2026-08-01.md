# LC114 phase-9-only larger-scale screen preregistration

LC113 proves that isolated phase-9 endpoint scales through `.10` preserve the
sole raw-index-9 entrant, exact transport, and the pre-target terminal count,
but remain 0/128 at raw index 10. The isolated head therefore has a measured
safe region unlike the destructive joint phase-8/9 branch.

Run one final bracket at scales `0,.30,.50,1`, retaining LC105 phase 8 exactly,
the 128-pair pairwise-256 complete-Puffer contract, seed 432140, raw-index-10
target, and 12,000-step bound. Select only a nonzero scale that creates one
pass without adding a pre-target terminal. If the bracket stays flat or loses
the raw-index-9 entrant, reject the LC111 phase-9 family without another scale
retry. FlightSim and Submission remain forbidden.
